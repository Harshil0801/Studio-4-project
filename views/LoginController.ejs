@Controller
public class LoginController {

    @PostMapping("/login")
    public String login(@RequestParam String username, @RequestParam String password, Model model) {
        // Simulate successful login (you can replace this with real authentication logic)
        if (username.equals("student") && password.equals("password")) {
            return "redirect:/role-selection";  // Redirect to role selection page
        }
        return "login";  // Login failed, show login page again
    }
}
